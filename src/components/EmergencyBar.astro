<div class="emergency-bar" id="emergency-bar" role="banner" aria-label="Informacion de emergencias">
  <div class="emergency-inner">
    <a href="tel:79504789" class="emergency-call" aria-label="Llamar a Emergencias 24/7: 7950-4789">
      <span class="emergency-pulse" aria-hidden="true"></span>
      <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
      </svg>
      <span class="emergency-label">Emergencias 24/7:</span>
      <strong class="emergency-phone">7950-4789</strong>
    </a>
    <div class="emergency-hours" aria-label="Horario de atencion">
      <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <circle cx="12" cy="12" r="10"/>
        <path d="M12 6v6l4 2"/>
      </svg>
      <span>Atención: Lun – Dom, 24 horas</span>
    </div>
  </div>
</div>

<style>
  .emergency-bar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    background-color: #D32F2F;
    height: 40px;
  }
  .emergency-inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
  }
  .emergency-call {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: #ffffff;
    min-height: 40px;
    transition: opacity 0.15s ease;
  }
  .emergency-call:hover { opacity: 0.85; }
  .emergency-call:focus-visible {
    outline: 2px solid rgba(255,255,255,0.8);
    outline-offset: 2px;
    border-radius: 4px;
  }
  .emergency-pulse {
    position: relative;
    width: 8px;
    height: 8px;
    flex-shrink: 0;
  }
  .emergency-pulse::before,
  .emergency-pulse::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 50%;
    background-color: #ffffff;
  }
  .emergency-pulse::after {
    animation: pulse-ring 1.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  @keyframes pulse-ring {
    0%   { transform: scale(1);   opacity: 0.9; }
    70%  { transform: scale(2.4); opacity: 0;   }
    100% { transform: scale(2.4); opacity: 0;   }
  }
  .emergency-label {
    font-size: 0.8125rem;
    font-weight: 500;
    opacity: 0.92;
    letter-spacing: 0.02em;
    white-space: nowrap;
  }
  .emergency-phone {
    font-size: 0.9375rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }
  .emergency-hours {
    display: none;
    align-items: center;
    gap: 0.375rem;
    color: rgba(255,255,255,0.92);
    font-size: 0.75rem;
    font-weight: 400;
    white-space: nowrap;
  }
  @media (min-width: 768px) {
    .emergency-inner { justify-content: space-between; }
    .emergency-hours { display: flex; }
  }
  @media (max-width: 359px) {
    .emergency-label { display: none; }
  }
  @media (prefers-reduced-motion: reduce) {
    .emergency-pulse::after { animation: none; }
  }
</style>
